# SPDX-License-Identifier: Apache-2.0
#
# description: (_.pid > 10) desugars to fun __x -> __x.pid > 10
# mode: structured
# expect: [42; 100]

let pids =
    ps |> filter (_.pid > 10)
        |> map _.pid

print pids
