# SPDX-License-Identifier: Apache-2.0
#
# description: Verify left-to-right execution order: 5 |> f |> g |> h
# expect: 60

let f (x: int) = x * 2
let g (x: int) = x + 10
let h (x: int) = x * 3
print (5 |> f |> g |> h)
