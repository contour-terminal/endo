# SPDX-License-Identifier: Apache-2.0
#
# description: Closure captures a string from enclosing scope (type annotation is `str` not `string`)
# expect: hello world

let prefix = "hello "; let greet (name: str) = prefix + name; print (greet "world")
