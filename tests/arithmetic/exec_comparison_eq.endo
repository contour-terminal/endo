# SPDX-License-Identifier: Apache-2.0
#
# description: Comparison via guard expression (avoids matching on boolean directly)
# expect: 1

let r =
    match 0 with
    | _ when 5 == 5 -> 1
    | _ -> 0

print r
