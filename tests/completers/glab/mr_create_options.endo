# SPDX-License-Identifier: Apache-2.0
#
# description: GLab completer: mr create options
# skip: Pending cross-Runner object pool support in executeSession
# source-file: data/completers/glab.endo
# expect: --title
# expect: --description
# expect: --source-branch
# expect: --target-branch
# expect: --label
# expect: --assignee
# expect: --reviewer
# expect: --milestone
# expect: --draft
# expect: --squash-before-merge
# expect: --remove-source-branch
# expect: --web
# expect: --fill
# expect:

let _ = glab_complete ["mr"; "create"] "-" |> each println
