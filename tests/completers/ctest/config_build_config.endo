# SPDX-License-Identifier: Apache-2.0
#
# description: CTest completer: config via --build-config
# skip: Pending HOF typed object support for CompletionEntry in each println
# source-file: data/completers/ctest.endo
# expect: Debug
# expect: Release
# expect: RelWithDebInfo
# expect: MinSizeRel
# expect:

let _ = ctest_complete ["--build-config"] "" |> each println
