# SPDX-License-Identifier: Apache-2.0
#
# description: Generic union with unit constructor match
# expect: empty

type Box<'a> =
    | Wrap of 'a
    | Empty

let b = Empty
match b with
| Wrap _ -> print "wrapped"
| Empty -> print "empty"
