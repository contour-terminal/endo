# SPDX-License-Identifier: Apache-2.0
#
# description: Simple generic union type with type erasure
# expect: 42

type Box<'a> =
    | Wrap of 'a
    | Empty

let b = Wrap 42

match b with
| Wrap x -> print x
| Empty -> print 0
