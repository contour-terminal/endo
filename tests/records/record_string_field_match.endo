# SPDX-License-Identifier: Apache-2.0
#
# description: String field via pattern matching
# expect: Charlie

type Person = { name: string; age: int }

let p = { name = "Charlie"; age = 35 }

print (match p with
| { name; age } -> name)
