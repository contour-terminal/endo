# SPDX-License-Identifier: Apache-2.0
#
# description: Let destructuring followed by record update on the same type
# expect: { x = 4; y = 6 }

type Point = { x: int; y: int }

let p1 = { x = 3; y = 4 }

let { x; y } = p1

let p2 = { p1 with x = p1.x + 1; y = p1.y + 2 }

print p2
