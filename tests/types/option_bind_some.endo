# SPDX-License-Identifier: Apache-2.0
#
# description: Option.bind applies function that returns Option
# expect: 5

let half (x: int) =
    if x % 2 == 0 then Some (x / 2)
    else None

print (Option.bind half (Some 10) ?| 0)
