# SPDX-License-Identifier: Apache-2.0
#
# description: Option.bind where f returns None
# expect: 0

let half (x: int) =
    if x % 2 == 0 then Some (x / 2)
    else None

print (Option.bind half (Some 3) ?| 0)
