# SPDX-License-Identifier: Apache-2.0
#
# description: Structured jobs filter placeholder
# mode: structured
# expect: [make build]

let cmds = jobs |> filter (_.state == "Done") |> map _.command

print cmds
