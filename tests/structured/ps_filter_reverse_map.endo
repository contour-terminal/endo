# SPDX-License-Identifier: Apache-2.0
#
# description: filter ppid == 1 (alice, bob), reverse, map to user
# mode: structured
# expect: [bob; alice]

print (ps |> filter (_.ppid == 1) |> reverse |> map (_.user))
