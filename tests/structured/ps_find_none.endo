# SPDX-License-Identifier: Apache-2.0
#
# description: find a process with pid > 1000 — should be None
# mode: structured
# expect: none

match ps |> find (fun p -> p.pid > 1000) with
| Some p -> print p.pid
| None -> print "none"
