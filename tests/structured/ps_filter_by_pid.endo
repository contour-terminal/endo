# SPDX-License-Identifier: Apache-2.0
#
# description: Filter processes with pid > 10 — should get 2 (pid=42, pid=100)
# expect: 2

let filtered = ps |> filter (fun p -> p.pid > 10)

print (length filtered)
