# SPDX-License-Identifier: Apache-2.0
#
# description: Pipeline: filter by size > 100 then map to name
# mode: structured
# expect: [docs; script.sh]

let names = ls |> filter (_.size > 100) |> map (_.name)

print names
