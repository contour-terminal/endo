# SPDX-License-Identifier: Apache-2.0
#
# description: Access FileMode.isExecutable property through FileInfo
# mode: structured
# expect: [true; false; true]

print (ls |> map (fun f -> f.mode.isExecutable))
