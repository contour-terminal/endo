# SPDX-License-Identifier: Apache-2.0
#
# description: Pipeline composition: filter by pid then map to command
# mode: structured
# expect: [firefox; vim]

let cmds = ps |> filter (_.pid > 10) |> map (_.command)

print cmds
