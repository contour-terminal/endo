# SPDX-License-Identifier: Apache-2.0
#
# description: Store ps result, then pipeline
# mode: structured
# expect: [42; 100]

let procs = ps

let highPid = procs |> filter (_.pid > 1) |> map (_.pid)

print highPid
