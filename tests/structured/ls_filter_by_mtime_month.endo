# SPDX-License-Identifier: Apache-2.0
#
# description: Filter files by mtime month and get names
# mode: structured
# expect: [docs; hello.txt; script.sh]

let names = ls |> filter (_.mtime.month == 11) |> map (_.name)

print names
