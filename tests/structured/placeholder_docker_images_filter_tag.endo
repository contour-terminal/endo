# SPDX-License-Identifier: Apache-2.0
#
# description: Placeholder docker images filter tag
# mode: structured
# expect: [nginx]

docker images |> filter (_.tag == "latest") |> map _.repository |> print
