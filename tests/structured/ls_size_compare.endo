# SPDX-License-Identifier: Apache-2.0
#
# description: Filter ls entries by Size comparison with literal
# mode: structured
# expect: [docs; script.sh]

print (ls |> filter (_.size > 100B) |> map (_.name))
