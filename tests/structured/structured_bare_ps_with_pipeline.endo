# SPDX-License-Identifier: Apache-2.0
#
# description: ps with pipeline should route through F# path (mock PIDs: 1, 42, 100)
# mode: structured
# expect: [1; 42; 100]

let r = ps |> map (_.pid)

print r
