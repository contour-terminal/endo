# SPDX-License-Identifier: Apache-2.0
#
# description: reduce via mapped pids: 1 + 42 + 100 = 143
# mode: structured
# expect: 143

match ps |> map (_.pid) |> reduce (fun a b -> a + b) with
| Some v -> print v
| None -> print "none"
