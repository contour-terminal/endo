# SPDX-License-Identifier: Apache-2.0
#
# description: 3-stage pipeline: filter pid > 1, sortBy mem, map to command
# mode: structured
# expect: [vim; firefox]

print (ps |> filter (_.pid > 1)
    |> sortBy (_.mem)
    |> map (_.command))
