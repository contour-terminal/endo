# SPDX-License-Identifier: Apache-2.0
#
# description: Docker ps pipeline chain
# mode: structured
# expect: [abc123def]

docker ps |> filter (_.status |> contains "Up") |> map (_.id) |> take 1 |> print
