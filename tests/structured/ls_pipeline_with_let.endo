# SPDX-License-Identifier: Apache-2.0
#
# description: Store ls result, then pipeline
# mode: structured
# expect: [docs; script.sh]

let files = ls

let bigFiles =
    files
        |> filter (fun f -> f.size > 100)
        |> map (fun f -> f.name)

print bigFiles
