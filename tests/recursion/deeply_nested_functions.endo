# SPDX-License-Identifier: Apache-2.0
#
# description: Function inside function inside function
# expect: 111

let a (x: int) =
    let b (y: int) =
        let c (z: int) = z + y + x
        c 1
    print (b 10)

a 100
