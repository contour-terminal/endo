# SPDX-License-Identifier: Apache-2.0
#
# description: Pattern match on ?-returning function with Some value
# expect: 42

let f x = x?

let r =
    match (f (Some 42)) with
    | Some n -> n
    | None -> 0

print r
