# SPDX-License-Identifier: Apache-2.0
#
# description: Longer list to exercise multiple loop iterations with cons construction.
# expect: [5; 4; 3; 2; 1]

let rec revAcc acc xs =
    match xs with
    | [] -> acc
    | h :: t -> revAcc (h :: acc) t

print (revAcc [] [1; 2; 3; 4; 5])
